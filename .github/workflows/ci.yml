name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [master]

jobs:
  check:
    uses: ./.github/workflows/_ci-checks.yml
    secrets:
      stripe_key: ${{ secrets.VITE_STRIPE_PUBLISHABLE_KEY }}
